//padEnd and padStart will add wuathever value to the string as far as it gets the same number of values
let example = 'Dylan';

console.log(example.padStart(10, 'a'));
console.log(example.padEnd(10, 'a'));
//We can also just pass a number to the parameter to add empty characters